sshpass -p '115049' ssh wei@192.168.1.84

# Navigate to project
cd ExoGlove-YOLOv8

# Activate environment
source exoglove_env/bin/activate

# Convert to rpk (Your original method - FIXED for Pi)
# Run this on Raspberry Pi (IMX export only works on Linux)
python3 export_imx_pi.py

# Then create .rpk package
imx500-package -i best_imx_model/packerOut.zip -o final_output

# Method 1: Python Picamera2 (Basic)
python3 exoglove_picamera2_simple.py

Method 2: rpicam-vid + IMX500 NPU (Recommended)
rpicam-vid --width 640 --height 640 --mode 2028:1520:10:U --post-process-file exoglove_imx500_config.json --output /tmp/exoglove_output.h264 --timeout 10000